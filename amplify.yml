version: 0.1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    build:
      commands: [cd my-site, npm run build]
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /my-site/public
    files:
      - '**/*'
  cache:
    paths: []
